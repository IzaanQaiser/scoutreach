# Copy to .env.local (gitignored) and fill in.

# Hunter.io — free plan works (real API access, 50 credits/mo, no card).
# Get a key at https://hunter.io -> account settings -> API.
HUNTER_API_KEY=

# Optional overrides — sensible defaults exist without these.
# SCOUTREACH_DB_PATH=scoutreach.db
# SCOUTREACH_CACHE_DIR=.cache
